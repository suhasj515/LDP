<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
	   		http://www.springframework.org/schema/beans/spring-beans.xsd
	   		http://www.springframework.org/schema/context
	   		http://www.springframework.org/schema/context/spring-context.xsd">

        <!-- load properties file -->
        <context:property-placeholder location="Sport.properties" />

        <bean id="myFortuneService"
                class="com.ldp.springldp.FortuneServiceLucky">
        </bean>

        <bean id="myCoach"
                class="com.ldp.springldp.TrackCoach">
                <!-- constructor injection -->
                <constructor-arg ref="myFortuneService" />
        </bean>

        <bean id="myCricketCoach"
                class="com.ldp.springldp.CricketCoach">
                <!--set up setter injection-->
                <property name="fortuneServiceLucky" ref="myFortuneService" />

                <!-- literal value injections -->
                <property name="emailAddress" value="${coach.email}" />
                <property name="team" value="${coach.team}"/>
        </bean>

</beans>